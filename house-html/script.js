function toggleMenu() {
    const burger = document.querySelector('.burger');
    const menu = document.querySelector('.menu');
    
    burger.classList.toggle('active'); // Меняем состояние кнопки
    menu.classList.toggle('active');  // Показываем или скрываем меню
  }
 

